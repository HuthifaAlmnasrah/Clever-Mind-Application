<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clevermindpob">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/clever_mind_pob_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/clever_mind_pob_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.CleverMindPOB">
        <activity
            android:name=".Activites.RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".Activites.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".Activites.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activites.MainActivity"
            android:exported="true"/>
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/> <!-- optional (needed if default theme has no action bar) -->
    </application>

</manifest>